| ID              | Título                             | Objetivo                                                                     | Pré-condições                                                                                            | Entradas/Parâmetros                                                         | Passos                                                                                | Validações esperadas                                                                                                                                                                                  | Observações                                                                                                            |
| --------------- | ---------------------------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **TC-POST-001** | Criar post – cenário feliz         | Garantir que a criação retorna sucesso e contrato válido.                    | `BASE_URL=https://jsonplaceholder.typicode.com` (ou alvo equivalente); schema `PostResponse` disponível. | `path="/posts"`; `payload = make_valid_post()` (title/body/userId válidos). | 1) Enviar `POST /posts` com `payload`.                                                | **Status 201**; Anexo **Expected** + **Expected JSON** (payload) + **Response JSON**; Parse com `PostResponse(**data)` sem erro; `title`, `body`, `userId` iguais aos enviados; **`id` inteiro > 0**. | Usa `assert_status_and_attach` e validação de contrato com Pydantic.                                                   |
| **TC-POST-002** | Rota inválida                      | Garantir que rotas incorretas não aceitam criação.                           | `BASE_URL=https://jsonplaceholder.typicode.com` (ou alvo equivalente); schema `PostResponse` disponível.                                                                                          | `path="/postsss"`; `json={"title":"x"}`.                                    | 1) Enviar `POST /postsss` com JSON simples.                                           | **Status 404**; Anexo **Expected** (sem Response JSON).                                                                                                                                               | No código: `attach_response_json=False` para não anexar corpo.                                                         |
| **TC-POST-003** | Body vazio `{}`                    | Verificar comportamento quando envia objeto vazio.                           | `BASE_URL=https://jsonplaceholder.typicode.com` (ou alvo equivalente); schema `PostResponse` disponível.                                                                                          | `path="/posts"`; `json={}`.                                                 | 1) Enviar `POST /posts` com `{}`.                                                     | **Status 404**; Anexos **Expected** + **Response JSON**; corpo contém **`id` inteiro > 0** (`_assert_has_id`).                                                                                        | Observação: valida presença de `id` mesmo em 404 conforme implementação atual.                                         |
| **TC-POST-004** | Sem body (`None`)                  | Verificar comportamento quando não há corpo.                                 | `BASE_URL=https://jsonplaceholder.typicode.com` (ou alvo equivalente); schema `PostResponse` disponível.                                                                                          | `path="/posts"`; `json=None`.                                               | 1) Enviar `POST /posts` sem body.                                                     | **Status 404**; Anexos **Expected** + **Response JSON**; corpo contém **`id` inteiro > 0**.                                                                                                           | Mantém validação de `id` via `_assert_has_id`.                                                                         |
| **TC-POST-005** | Body parcial (apenas `title`)      | Verificar criação com payload incompleto.                                    | `BASE_URL=https://jsonplaceholder.typicode.com` (ou alvo equivalente); schema `PostResponse` disponível.                                                                                          | `path="/posts"`; `payload={"title":"apenas título"}`.                       | 1) Enviar `POST /posts` com apenas `title`.                                           | **Status 404**; Anexos **Expected** + **Expected JSON** (payload) + **Response JSON**; corpo contém **`id` inteiro > 0**; se resposta incluir `title`, deve ser igual ao enviado.                     | Mantém checagem condicional de eco do `title`.                                                                         |
| **TC-POST-006** | Payloads inválidos (parametrizado) | Validar que, mesmo com payloads inválidos, o serviço retorna criação com ID. | Coleção `INVALID_PAYLOADS` definida (tipos errados, campos faltando, etc.).              | `post_id ∈ INVALID_PAYLOADS` (cada item da lista).                          | 1) Para cada `payload`, enviar `POST /posts` com o item; 2) Anexar Expected/Response. | **Status 404**; **`id` inteiro > 0** em todas as respostas.                                                                                                                                           | Parametrizado por `@pytest.mark.parametrize("payload", INVALID_PAYLOADS)`; anexa também o **Expected JSON** (payload). |

