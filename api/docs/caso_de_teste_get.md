| ID             | Título                                         | Objetivo                                                                     | Pré-condições                                                                             | Entradas/Parâmetros                                            | Passos                                            | Validações esperadas                                                                                                                                                                                            | Casos gerados                                                                                                  |
| -------------- | ---------------------------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **TC-GET-001** | Listar todos os posts                          | Verificar que a listagem responde com sucesso.                               | `BASE_URL=https://jsonplaceholder.typicode.com`; helper `assert_status_and_attach` ativo. | `path="/posts"`                                                | 1) Enviar `GET /posts`.                           | **Status 200**; anexos no report (**Expected** + **Response JSON**). *(Opcional endurecer: `Content-Type` contém `application/json`; `resp.json()` é `list`)*                                                   | —                                                                                                              |
| **TC-GET-002** | Obter post por **ID válido** (parametrizado)   | Garantir que cada ID válido retorna o recurso correto com campos essenciais. | `BASE_URL=https://jsonplaceholder.typicode.com`; helper `assert_status_and_attach` ativo. cima.                                                                           | `post_id ∈ VALID_POST_IDS` (ex.: `["1","2","3"]`)              | 1) Enviar `GET /posts/{post_id}` para cada valor. | **Status 200**; anexos (**Expected** + **Response JSON** + *(opcional)* **Expected Response JSON**=`{"id": int(post_id)}`); corpo é `dict`; `data["id"] == int(post_id)`; presença de **`title`** e **`body`**. | **TC-GET-002-1** (`id="1"`), **002-2** (`"2"`), **002-3** (`"3"`), conforme `VALID_POST_IDS`.                  |
| **TC-GET-003** | Obter post por **ID inválido** (parametrizado) | Validar tratamento de IDs inválidos como não encontrado.                     | `BASE_URL=https://jsonplaceholder.typicode.com`; helper `assert_status_and_attach` ativo. cima.                                                                           | `post_id ∈ INVALID_IDS = ["9999","0","-1","abc","null"]`       | 1) Enviar `GET /posts/{post_id}` para cada valor. | **Status 404**; anexo **Expected** no report. *(Corpo pode variar; não é exigido)*                                                                                                                              | **TC-GET-003-A** (`"9999"`), **003-B** (`"0"`), **003-C** (`"-1"`), **003-D** (`"abc"`), **003-E** (`"null"`). |
| **TC-GET-004** | **Rotas inválidas** (parametrizado)            | Verificar que caminhos incorretos retornam não encontrado.                   | `BASE_URL=https://jsonplaceholder.typicode.com`; helper `assert_status_and_attach` ativo. cima.                                                                           | `path ∈ INVALID_ROUTES = ["/postsss/1", "/wrong", "/posts/%"]` | 1) Enviar `GET {path}` para cada item.            | **Status 404**; anexo **Expected** no report.                                                                                                                                                                   | **TC-GET-004-1** (`/postsss/1`), **004-2** (`/wrong`), **004-3** (`/posts/%`).                                 |
